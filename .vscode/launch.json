{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run qimgv (novideo, X11) — lldb",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build-novideo/qimgv/qimgv",
      "args": ["$HOME/Pictures"],
      "cwd": "${workspaceFolder}/build-novideo/qimgv",
      "env": { "QT_QPA_PLATFORM": "xcb" },
      "preLaunchTask": "Build (novideo)",
      "initCommands": [
        "settings set target.process.thread.step-avoid-libraries true"
      ]
    }
  ]
}
